package dev.lrxh.neptune.config;

/*public class ConfigReaderWriter {

    <T> T getValueOf(MenusLocale configItem) {
        IConfig config = this.getConfigReaderStrategy(configItem.getDataType());

        return (T) config.getDefaultValue();
    }

    private <T> IConfig<T> getConfigReaderStrategy(DataType type) {
        switch (type) {
            case STRING:
                return (IConfig<T>) new StringConfigImpl();
            case INT:
                return new IntConfigImpl();
        }
   }
}*/
